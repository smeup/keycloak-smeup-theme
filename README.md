# Keycloak Smeup Theme

## Custom theme for Keycloak based on Smeup design system.

## Docker
To launch keycloak on localhost with the custom theme, run the following command:

```sh
docker compose up
```

Realm settings --> themes --> set 'smeup' on themes voices
identity providers --> client ID & client Secret ( generic values )
